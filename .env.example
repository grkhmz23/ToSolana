# Database (SQLite for local dev, swap to postgres:// for production)
DATABASE_URL="file:./dev.db"

# Solana RPC endpoint (defaults to mainnet-beta if not set)
NEXT_PUBLIC_SOLANA_RPC_URL="https://api.mainnet-beta.solana.com"

# Bridge provider API keys
# At least one must be set for quotes to work.

# Rango Exchange - get a key at https://app.rango.exchange/
RANGO_API_KEY=""
# Public API key for Universal Mode (Rango widget)
NEXT_PUBLIC_RANGO_API_KEY=""

# LI.FI - get a key at https://li.fi/ or use an integrator name
LIFI_API_KEY=""
# Alternative: set an integrator name instead of an API key
# LIFI_INTEGRATOR="your-app-name"

# WalletConnect - get a project ID at https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=""
